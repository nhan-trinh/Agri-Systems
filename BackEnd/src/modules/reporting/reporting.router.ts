import { Router } from 'express';
import { reportingController } from './reporting.controller';

const router = Router();

// Example: router.get('/', reportingController.getAll);

export default router;
