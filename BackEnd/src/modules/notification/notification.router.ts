import { Router } from 'express';
import { notificationController } from './notification.controller';

const router = Router();

// Example: router.get('/', notificationController.getAll);

export default router;
