import { Router } from 'express';
import { warehouseController } from './warehouse.controller';

const router = Router();

// Example: router.get('/', warehouseController.getAll);

export default router;
