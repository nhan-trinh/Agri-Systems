import { Router } from 'express';
import { checkvnQrController } from './checkvn-qr.controller';

const router = Router();

// Example: router.get('/', checkvnQrController.getAll);

export default router;
