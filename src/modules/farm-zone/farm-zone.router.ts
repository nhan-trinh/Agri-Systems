import { Router } from 'express';
import { farmZoneController } from './farm-zone.controller';

const router = Router();

// Example: router.get('/', farmZoneController.getAll);

export default router;
