import { Router } from 'express';
import { farmingLogController } from './farming-log.controller';

const router = Router();

// Example: router.get('/', farmingLogController.getAll);

export default router;
